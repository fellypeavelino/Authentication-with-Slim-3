<!DOCTYPE html>
	<html>
		<head>
			<meta charset="UTF-8">
			<title>
				Code
			</title>
			<link rel="stylesheet" href="{{ base_url() }}/css/bootstrap.min.css" />
			<link rel="stylesheet" href="{{ base_url() }}/css/app.cs" />
		</head>
		<body>

			{% include 'templates/partials/navigation.twig' %}

			<div class="container" >
				{% block content %}
				{% endblock %}
			</div>
			<script src="{{ base_url() }}/js/jquery-2.2.0.min.js"></script>
			<script src="{{ base_url() }}/js/bootstrap.min.js"></script>
		</body>
	</html>